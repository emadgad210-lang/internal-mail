# تشغيل بريد الشركة الداخلي كسيرفر على جهازك

## 1) ثبّت Node.js (مرة واحدة بس)
حمّل النسخة LTS من الموقع الرسمي: https://nodejs.org
ثبّته بشكل عادي (Next, Next, Finish).

## 2) شغّل السيرفر
- افتح الـ Terminal / Command Prompt.
- روح لمجلد `mail-server` (اللي فيه ملف `server.js`).
- اكتب:

```
node server.js
```

هتشوف رسالة إن السيرفر شغال على المنفذ 3000. سيب النافذة دي شغالة في الخلفية طول الوقت اللي عايز فيه الموظفين يستخدموا البريد (تقدر تصغّرها براحتك، بس متقفلهاش).

## 3) افتح الموقع من نفس الجهاز
افتح المتصفح واكتب:
```
http://localhost:3000
```

## 4) اعرف عنوان الـ IP بتاع الجهاز عشان باقي الموظفين يدخلوا
- على ويندوز: افتح Command Prompt واكتب `ipconfig` وشوف قيمة "IPv4 Address" (زي 192.168.1.15).
- على ماك: من System Settings > Network، أو اكتب في Terminal: `ipconfig getifaddr en0`

## 5) الموظفين يفتحوا من أجهزتهم (لازم يكونوا على نفس شبكة الواي فاي/اللان)
```
http://192.168.1.15:3000
```
(استبدل الرقم بعنوان الـ IP الحقيقي بتاع جهازك)

## ملاحظات مهمة
- الجهاز لازم يفضل شغال ومتصل بالشبكة طول وقت الاستخدام، وميدخلش Sleep.
- لو فايروول ويندوز سأل "Allow access?" لما تشغل السيرفر أول مرة، اختار Allow (خصوصًا لشبكة Private/Home).
- كل البيانات (الحسابات والرسائل) بتتخزن في ملف `data.json` جوه نفس المجلد — لو عايز تعمل نسخة احتياطية، يكفي تنسخ الملف ده.
- الجهاز نفسه يفضل شغال عادي جدًا في نفس الوقت (تصفح، شغل، برامج تانية) — السيرفر مايأثرش على الأداء، بس سيبه شغال في الخلفية وماتقفلش نافذة الـ Terminal.
